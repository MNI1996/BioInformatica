class DSSPService:
    def conservate(self, alignments):
        # aligments es una lista de clave valor, que es lo que se devuelve de clustal
        """
            for align in alignments:
                id_protein = align[0]
                seq = align[1]
                llamar a dssp que me traiga la conservación de seq
                result =recorrer seq y crear una variable que donde haya gaps complete con algo y sino que ponga lo que encuentra en dssp
                resultados.append(result)

            return resultados
        """
        pass